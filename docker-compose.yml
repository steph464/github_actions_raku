version: "3.9"

services:
  api_server:
    build:
      context: ./app_V2
      dockerfile: Dockerfile.app
    ports:
      - "8005:8000"
    volumes:
      - ./app_V2:/app
      - ./BDD:/BDD
      - ./models:/models

  test_ci:
    build:
      context: ./test_ci
      dockerfile: Dockerfile.test_ci
    volumes:
      - ./test_ci:/test_ci
    depends_on:
      - api_server

##############################################################
# Ajout de networks
networks:
  network_rakuten:
##############################################################
